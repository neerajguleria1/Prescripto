{
  "name": "prescripto",
  "version": "1.0.0",
  "description": "Healthcare appointment booking platform",
  "scripts": {
    "start": "concurrently \"cd frontend && npm run dev\" \"cd backend && npm start\" \"cd admin && npm run dev\"",
    "install-all": "cd backend && npm install && cd ../frontend && npm install && cd ../admin && npm install",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm run dev",
    "admin": "cd admin && npm run dev"
  },
  "dependencies": {
    "concurrently": "^9.2.0"
  }
}
